#include <stdio.h>

int main(){

    //constants are fixed values that cannot be altered by the program during execution    float pi = 3.14159;

    const float PI = 3.14159; // cosnt keyword is required 

    // naming convention for const variables - varible name should be all upper case (ex: PI) 

    printf("%f", PI);

    return 0;
}